import React from "react";
import { HeaderButton, HeaderStyle } from "./Header.styles";

export const Header = () => {
  return (
    <HeaderStyle>
      <HeaderButton>More Information</HeaderButton>
    </HeaderStyle>
  );
};
